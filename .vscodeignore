# Build files
.vscode/**
.vscode-test/**
src/**
out/**
node_modules/**
webpack.config.js
esbuild.config.js

# Development files
.gitignore
.git/**
tsconfig.json
tmp_rovodev_*
**/*.map

# Exclude large upstream repos (used only for investigation)
lean4-master/**
vscode-lean4-master/**

# Lean files (not needed in extension)
*.lean
flake.nix
flake.lock
acli

# Test and scripts (not needed in runtime)
scripts/**

# Documentation and misc
package-lock.json
*.vsix
README.md
